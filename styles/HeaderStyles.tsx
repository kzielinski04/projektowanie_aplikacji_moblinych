import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 20,
        backgroundColor: "#4a90e2",
    },
    title: {
        color: "white",
        fontSize: 20,
        fontWeight: "bold",
    },
});